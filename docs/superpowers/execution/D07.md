# D07 execution ledger

Plan: approved v3 native CLI workspace, W1/D07.
Base: 54b1f918fabe199f9e4e4a80c8e9c6f3140f546d (D06 #15).
D06 CI #142 / 35715228410 was read and confirmed completed/success before starting.

Status: IN_PROGRESS. Initial schema tests submitted before project implementation.
Isolation: dedicated stacked feature branch; no main writes, merge, release or native-data changes.
Execution: GitHub connector writes and GitHub Actions. Container has no Rust and git network DNS resolution failed; no local checkout or native-machine test claim.

Pre-flight: D06 produces WorkspaceRepository with independent lock, CAS revision and atomic writes; D07 adds registeredProjects under the same lock and must preserve profiles/unknown sections. D08 consumes the backend identity only, never a frontend lowercase approximation.
Ruling: retain D06 module boundaries (cli commands and api/cli-style wrappers) instead of expanding unrelated command facades. Public project operations validate caller before filesystem I/O.
Ruling: registration is an idempotent additive operation under the latest-document lock, not a read-then-unconditional overwrite. User metadata edits/removal require the observed workspace revision.
Ruling: verified directory identity comes from OS file identity. Missing/unresolvable aliases are not merged through lexical '..' reduction or platform-wide lowercase. Selected display paths remain distinct from verified keys.

Remaining: schema/transaction implementation; directory identity and alias tests; registration/metadata/remove APIs; frontend open-directory integration; complete CI and self-review.
