# D08 execution ledger

Plan: approved v3 W2/D08, supplied planning archive; PR06 begins here, D09 remains separate work.
Base: `4070c3ceac24564e96a31942ff5d2468a746af49`, D07 #16; CI #150 freshly verified completed/success.

## Constraints and rulings

- Dedicated stacked feature branch; no main writes, merge, release, package upgrade or native configuration mutation.
- Source plan/spec were recovered from the supplied v3 planning archive. Their complete source paths are not present in the remote branch; do not misrepresent them as committed files.
- Git clone in the container failed at DNS resolution and no local Rust toolchain is installed. GitHub Actions is the executable validation environment. No user-machine test is implied.
- Reuse actual D05 LaunchRequest and D06 Profile types; the plan's illustrative ResolvedProfile fixture is not a second persistent schema.
- D08 freezes backend launch inputs and exposes per-profile availability. D09 builds argv; D10 migrates the legacy shell launcher; D11 owns reservation and WebView-epoch authorization. Do not bypass those dependencies by wiring a new spawn path now.
- Legacy global startup checks/refresh remain on the old application route until its launcher/startup migration; the new per-profile route must not call them. This is an explicit integration gap, not complete app-startup acceptance.
- Environment priority: original OS environment, verified terminal declarations, legacyClaude-only legacy layer, profile overrides/deletions, explicitly enabled minimal observer fields. Preserve user-inherited provider variables. New observer injection defaults off.
- Snapshot secrets must not implement serialization; debug output must redact values. Profile lookup checks profile revision, not workspace revision.

## Observed RED

CI #151, run `35722366224`, commit `14fe2d798e4904a986343589aebcebe3ec681009`, Rust job `106727964944`: compilation succeeded, then all 10 new Windows environment tests failed as intended. Library: 363 passed / 10 failed / 14 ignored. Clippy passed. Formatting of the new tests also failed, tracked separately from behavioral RED. Frontend job passed. Full Rust log was read before environment implementation.

The next commit implements the tested environment rules and introduces the snapshot interface scaffold plus eight behavior tests. Snapshot scaffold deliberately returns SNAPSHOT_NOT_IMPLEMENTED, and discovery returns no candidates; its actual behavioral RED must be observed before implementation.

Status: IN_PROGRESS; no completion or independent-review claim.
