# D08 execution ledger

Plan: approved v3 `docs/superpowers/plans/native-cli/W2.md`, D08 only (PR06 begins here; D09 remains separate work).
Base: `4070c3ceac24564e96a31942ff5d2468a746af49`, D07 #16; CI #150 freshly verified completed/success.

## Constraints and rulings

- Dedicated stacked feature branch; no main writes, merge, release, package upgrade or native configuration mutation.
- Source plan/spec were recovered from the supplied v3 planning archive. They are not present at the same paths in the remote branch; do not misrepresent them as committed source.
- Git clone in the container failed at DNS resolution and no local Rust toolchain is installed. GitHub Actions is the executable validation environment. No user-machine test is implied.
- Ruling: reuse actual D05 LaunchRequest and D06 Profile types; the plan's illustrative ResolvedProfile fixture is not a second persistent schema.
- Ruling: D08 freezes backend launch inputs and exposes per-profile availability. D09 builds argv; D10 migrates the legacy shell launcher; D11 owns reservation and WebView-epoch authorization. Do not bypass those dependencies by wiring a new spawn path now.
- Ruling: legacy global startup checks/refresh remain on the old application route until its launcher/startup migration; the new per-profile route must not call them. This is an explicit integration gap, not complete app-startup acceptance.
- Environment priority: original OS environment, verified terminal declarations, legacyClaude-only legacy layer, profile overrides/deletions, enabled minimal observer fields. Preserve user-inherited provider variables.
- Snapshot secrets must not implement serialization; debug output must redact values. Profile lookup checks profile revision, not workspace revision.

## Evidence

Initial environment interface scaffold returns the original environment only. Tests exercise real production entry points; behavioral RED must be observed before implementation. Snapshot and availability cases follow before their respective implementation.

Status: IN_PROGRESS; no completion or independent-review claim.
