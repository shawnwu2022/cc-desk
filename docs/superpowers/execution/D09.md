# D09 execution ledger — approved v3 W2/native invocation adapters

Base: `55d0d80a3b7478240d1afd4f388a8a88694ec48c`, PR #17, CI #156 freshly confirmed completed/success. Continue on the existing isolated feature branch `feat/native-cli-launch-snapshot`; D08 and D09 belong to planned PR06. No main writes, merge, release, version/lockfile changes or real CLI launches.

## Source and pre-flight

The supplied planning ZIP `cc-desk-development-plan-v3-2026-09-22.zip` contains W2/D09 and the v3 contracts. Indexed Files retrieval returned the master plan but not the embedded task card; the mounted ZIP was read directly. The task is not a newly invented plan. Source archive content is not claimed to be already committed at its planning path.

- D08 -> D09: actual LaunchSnapshot owns the complete OS-string environment and authoritative request. Ruling: CliInvocation borrows that snapshot, exposes its complete environment, and stores only the constructed argv. Do not introduce a second env_set/env_remove authority. D10 MUST clear the process environment before applying the complete map; otherwise explicit deletions would be undone.
- D09 -> D10: invocation contains logical CLI program/argv plus the frozen launcher/runner. Ruling: no quoting, shell interpolation, filesystem refresh or actual spawn here. Platform encoding and process proof remain D10.
- D09 -> D11/D12: the requested locator is not a verified session ID. Ruling: initial effective cwd, config root and native session stay unknown, even with no extra flags; the CLI or shell can choose them. Preserve launchCwd separately.
- D09 step 3.1 conflicts with the staged D08 ruling that keeps the old spawn path until D10/D11. Ruling: implement all new argument semantics in these adapters now, and move/remove legacy PTY construction when its launcher is switched; do not silently bypass initialization or change the current paste route. This carry-forward is an integration requirement, not a claim that PtyManager is already generic.
- Legacy defaultCustomArgs is opaque shell text, not argv. Ruling: nonempty inherited text causes LEGACY_ARGUMENTS_REQUIRE_MIGRATION in the new structured builder. Empty text, an explicit array/unset, or raw remains possible. The existing legacy route is not changed. D10 must provide explicit migration, not split on spaces or discard text.
- Only explicit Claude skipPermissions=true adds its documented flag. No Codex permission/model/sandbox/alternate-screen defaults or Desk observer plugin injection is added. User-supplied args remain user's native CLI input, not a Desk safety policy or a blacklist. Observer plumbing remains D13.
- Structured action prefix is followed by explicit profile arguments and explicit extra arguments, each in original order. Native parsers remain authoritative: caller-supplied flags/positionals may change native behavior. In particular, Claude --resume has an optional value, so a following caller positional can select a session instead of the picker. The wrapper does not silently reorder or drop that positional. Use raw for exact complete command ordering.
- Claude all-project picker has no verified mapping in this task; return UNSUPPORTED_ACTION for that GUI action, never invent Codex's --all. Raw/native picker remains available.

Official references freshly checked 2026-09-23:
- https://developers.openai.com/codex/cli/reference (redirects to https://learn.chatgpt.com/docs/developer-commands?surface=cli): codex resume, resume --all, resume SESSION_ID; locator accepts UUID/session name.
- https://code.claude.com/docs/en/cli-reference: --resume ID/name or picker; --dangerously-skip-permissions is explicit. These are command-contract sources, not tested local binary versions or native certification.

Container has Node but no Rust toolchain; `git ls-remote` failed DNS (exit 128). Use the authorized GitHub connection for source writes and Actions for executable checks, not downloadable patches or claims about the user's machine.

## Observed RED

Commit `51e0cacabcde8cafce921e1469244ebfc807b3ee`, CI #157 (`35802486788`), PR merge `8a4cf525e617fad0ab652976317fa6c60ee2b6b8`:

- Rust job `106995740969` compiled successfully, then all 17 common D09 tests failed against INVOCATION_NOT_IMPLEMENTED. Library result: **398 passed / 17 failed / 14 ignored**. The full log was read before implementation.
- The one Unix-specific case did not execute on the Windows runner and is not claimed as observed RED or accepted.
- Rustfmt reported layout in the new test file. Strict Clippy rejected a Path import used only by the Unix case. These are test-maintenance failures, not the behavioral RED evidence; the import is qualified at its Unix-only use and formatting corrected without changing assertions.
- The frontend job `106995740742` completed successfully. Its step status was read; no fresh detailed frontend count is claimed at this checkpoint.

## Implementation checkpoint

The common builder verifies complete request equality against the frozen snapshot, then dispatches to independent Claude/Codex/Shell action mappers. Raw uses only the frozen raw argv. Arguments are never parsed, escaped, normalized or logged here. The invocation borrows program/runner, caller ownership, cwd and the complete environment rather than resolving them again. RunPublicIdentity has a constructor that keeps all effective native fields unknown until observed.

## Task checklist

- [x] Read task/spec and current repository boundary; confirm base CI.
- [x] Observe invocation behavior RED.
- [x] Implement Claude/Codex/Shell adapters and unknown initial identity.
- [ ] Read full CI results; fix actual failures without weakening assertions.
- [ ] Self-review and record exact verified head; independent review not available here.
- [x] Carry D10 platform/legacy integration requirements forward.

Status: implementation submitted for validation, not GREEN or complete native acceptance. Final verification belongs to the exact tested commit and PR metadata, not an unverified docs-only successor.
